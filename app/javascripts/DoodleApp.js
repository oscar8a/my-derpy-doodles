class DoodleApp {
    //Launches Doodle App
    constructor() {
        console.log('in the DoodleApp constructor')
            // We could Call Drawing APP here
        console.log('Calling UserLogin()')
        new UserLogin();
    }
}